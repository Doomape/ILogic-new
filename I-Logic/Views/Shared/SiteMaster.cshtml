<!DOCTYPE html>
<!--[if !IE 7]>
	<style type="text/css">
		#wrap {display:table;height:100%}
	</style>
<![endif]-->
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <script src="@Url.Content("~/Scripts/jquery-1.7.1.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/interface.js")" type="text/javascript"></script>
    <link rel="stylesheet" href="@Url.Content("~/Content/Site.css")" type="text/css" />
    <link rel="stylesheet" href="http://code.jquery.com/ui/1.10.0/themes/base/jquery-ui.css" />
    <script src="http://code.jquery.com/ui/1.10.0/jquery-ui.js"></script>

</head>

<body>
    <div id = "wrapper">
        <div id = "top-row">
           <div onclick="redirectAsp()" class="mouse_hover" style="float:left;position:relative;width:340px;height:100px;border-right: 1px solid black; text-align: center;"><img src="/Content/images/171545_VS_Purp526_rgbtran.png" /></div>
             <div onclick="redirectElectronic()" class="mouse_hover" style="float:left;position:relative;width:340px;height:100px;border-right: 1px solid black;"></div>
             <div onclick="redirectPhp()" class="mouse_hover" style="float:left;position:relative;width:341px;height:100px;"></div>
        </div>
        <div id = "content-row">
           @RenderBody()
        </div>
        <div id="bottom-row">
            <div id="open_twitter_form"></div>
            <div id ="twitter_form"></div>
            <div class="dock" id="dock">
                <div class="dock-container">
                    <a class="dock-item" href="/Site/Index"><img src="~/Content/images/home.png" alt="home" /><span>Почетна</span></a> 
                    <a class="dock-item" href="/Site/Products"><img src="~/Content/images/proizvodi.png" alt="portfolio" /><span>Производи</span></a> 
    @*                <a class="dock-item" href="Novosti.aspx"><img src="~/Content/images/novosti.png" alt="music" /><span>Новости</span></a>  *@
                    <a class="dock-item" href="zaNas.aspx"><img src="~/Content/images/aboutUs.png" alt="music" /><span>За нас</span></a> 
                    <a class="dock-item" href="Kontakt.aspx"><img src="~/Content/images/email.png" alt="contact" /><span>Контакт</span></a>
                </div>
            </div>
            <div id="mail_form"></div>
        </div>
    </div>



    <script type="text/javascript">

        var con = document.getElementById("content-row").offsetHeight;
        var top = document.getElementById("top-row").offsetHeight
        var body = document.body.offsetHeight;
        if ((con + top) < body) { document.getElementById("bottom-row").style.position = "fixed"; }

        $(document).ready(
		function meni() {
		    $('#dock').Fisheye(
				{
				    maxWidth: 15,
				    items: 'a',
				    itemsText: 'span',
				    container: '.dock-container',
				    itemWidth: 60,
				    proximity: 30,
				    halign: 'center'
				}
			)
		}
	);
        var hiddenContent = $('#open_twitter_form');
        $("#twitter_form").click(function(event){
 
            // Cancel the default event (this isn't a real link).
            event.preventDefault();
 
            // Check to see if the content is visible.
            if (hiddenContent.is( ":visible" )){
 
                // Hide it slowly.
                if (hiddenContent.css("height") == "500px") {
                    hiddenContent.slideUp(2000);
                }
 
            } else {
 
                // Show it slowly.
                if (hiddenContent.css("height")=="500px") {
                    hiddenContent.slideDown(2000);
                }
 
            }
 
            
        });
 
        function redirectAsp()
        {
            window.location.href = "/Site/Index_aspNet";
        }   

        function redirectElectronic()
        {
            window.location.href = "/Site/Index";
        } 
        function redirectPhp()
        {
            window.location.href = "/Site/Index_php";
        } 
            //if ($('#open_twitter_form').css('display') == 'none' || $('#open_twitter_form').css("height") == "0px") {
            //    $('#open_twitter_form').slideDown(2000, function () {
            //        $('#open_twitter_form').css("height", "500px");
            //    });

            //}
            //else {

            //    $('#open_twitter_form').remove();
            //    $("#footer").append("<div id='open_twitter_form'></div>");
            //    $('#open_twitter_form').slideUp(2000, function () {
            //        //$('#open_twitter_form').css("height", "0px");
            //       // $('#open_twitter_form').css('display', "none");
            //    });
        //}
       
    </script>
</body>
</html>
